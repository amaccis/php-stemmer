language: php

services:
  - docker

before_install:
  - docker run --name php-libstemmer -d -v $PWD:/var/www amaccis/php-libstemmer

script:
  - docker exec php-libstemmer composer install
  - docker exec php-libstemmer ./vendor/bin/phpunit